# =====================================================================
# Exemple de fichier .env - COPIER ET PERSONNALISER
# Renommez ce fichier en '.env' et remplissez les valeurs
# NE PAS commiter le vrai .env sur Git (contient secrets!)
# =====================================================================

# =====================================================================
# DJANGO CORE SETTINGS
# =====================================================================

# Clé secrète Django (générez une nouvelle avec:
# python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
SECRET_KEY=change-this-to-a-secure-random-key-in-production

# Mode debug (False en production!)
DEBUG=True

# Domaines autorisés (séparés par des virgules)
ALLOWED_HOSTS=localhost,127.0.0.1,your-app-name.onrender.com

# =====================================================================
# DATABASE CONFIGURATION - RENDER.COM
# =====================================================================

# Option 1: Render.com (PostgreSQL automatique) - À PRÉFÉRER
# Render fournit automatiquement DATABASE_URL lors du déploiement
# Aucune configuration nécessaire, la ligne ci-dessous est optionnelle:
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Option 2: Développement LOCAL (SQLite)
DATABASE_ENGINE=django.db.backends.sqlite3

# Option 3: MySQL Legacy (cPanel) - DÉCOMMENTER ET REMPLIR SI NÉCESSAIRE
# DATABASE_ENGINE=django.db.backends.mysql
# DATABASE_NAME=aime_db
# DATABASE_USER=aime_user
# DATABASE_PASSWORD=votre_mot_de_passe_securise_ici
# DATABASE_HOST=localhost
# DATABASE_PORT=3306

# Option 4: PostgreSQL (autre que Render) - DÉCOMMENTER ET REMPLIR SI NÉCESSAIRE
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=aime_db
# DATABASE_USER=aime_user
# DATABASE_PASSWORD=votre_mot_de_passe_securise_ici
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# =====================================================================
# SECURITY SETTINGS (Production)
# =====================================================================

# Redirection SSL/HTTPS en production
# SECURE_SSL_REDIRECT=True

# Secure cookies
# CSRF_COOKIE_SECURE=True
# SESSION_COOKIE_SECURE=True

# =====================================================================
# CACHING SETTINGS
# =====================================================================

# Actuellement: Base de données (compatible cPanel)
# Alternative: Redis (nécessite VPS)
# Ne change généralement pas ce paramètre

# =====================================================================
# EMAIL CONFIGURATION (Optionnel)
# =====================================================================

# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=votre-email@gmail.com
# EMAIL_HOST_PASSWORD=votre-mot-de-passe-app

# =====================================================================
# LOGGING (Optionnel)
# =====================================================================

# LOG_LEVEL=INFO

# =====================================================================
# NOTES IMPORTANTES
# =====================================================================

# 1. DÉVELOPPEMENT LOCAL:
#    - Utilisez DATABASE_ENGINE=django.db.backends.sqlite3
#    - DEBUG=True est acceptable
#    - Conservez les autres par défaut

# 2. PRODUCTION CPANEL:
#    - Changez DATABASE_ENGINE=django.db.backends.mysql
#    - Remplissez DATABASE_NAME, DATABASE_USER, DATABASE_PASSWORD
#    - Générez une nouvelle SECRET_KEY
#    - Mettez DEBUG=False
#    - Activez les SECURE_* settings

# 3. SÉCURITÉ:
#    - NE JAMAIS committer .env sur Git
#    - Utilisez cet exemple .env.example comme template
#    - Sur le serveur, créez .env avec les vraies valeurs

# 4. GÉNÉRER UNE CLÉE SECRÈTE:
#    python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# =====================================================================
# POUR DÉMARRER:
# 1. Copiez ce fichier: cp .env.example .env
# 2. Éditez .env avec vos valeurs
# 3. Démarrez Django: python3 manage.py runserver
# =====================================================================
