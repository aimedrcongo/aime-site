name: Test FTP Connection

on:
  workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test FTP Secrets
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          echo "FTP Server: ${FTP_SERVER:-NOT SET}"
          echo "FTP Username: ${FTP_USERNAME:-NOT SET}"
          echo "FTP Password: ${FTP_PASSWORD:+SET}${FTP_PASSWORD:-NOT SET}"
          
          if [ -z "$FTP_SERVER" ] || [ -z "$FTP_USERNAME" ] || [ -z "$FTP_PASSWORD" ]; then
            echo "❌ ERROR: One or more secrets are missing!"
            exit 1
          else
            echo "✅ All secrets are set!"
          fi

      - name: Install and test lftp
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          sudo apt-get update && sudo apt-get install -y lftp
          
          echo "Testing FTP connection..."
          lftp -u "$FTP_USERNAME,$FTP_PASSWORD" "ftp://$FTP_SERVER" << 'EOF'
          set ssl:verify-certificate no
          pwd
          ls
          quit
          EOF
