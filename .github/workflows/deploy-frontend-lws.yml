name: Deploy Frontend to LWS

on:
  push:
    branches:
      - main
    paths:
      - 'main/templates/**'
      - 'main/static/**'
      - '.github/workflows/deploy-frontend-lws.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Deploy to LWS FTP
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          cat > deploy.sh << 'SCRIPT'
          #!/bin/bash
          set -e
          
          echo "Connecting to FTP server: $FTP_SERVER"
          
          lftp -u "$FTP_USERNAME,$FTP_PASSWORD" "ftp://$FTP_SERVER" << EOF
          set ssl:verify-certificate no
          cd public_html
          echo "Uploading templates..."
          mirror -R --delete --verbose main/templates templates
          echo "Uploading static files..."
          mirror -R --delete --verbose main/static static
          quit
          EOF
          
          SCRIPT
          
          chmod +x deploy.sh
          ./deploy.sh
