name: Deploy Frontend to LWS

on:
  push:
    branches:
      - main
    paths:
      - 'main/templates/**'
      - 'main/static/**'
      - '.github/workflows/deploy-lws.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Deploy to FTP via lftp
        run: |
          lftp -e "
          set ftp:ssl-allow no
          open ftp://${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}@${{ secrets.FTP_SERVER }}
          cd public_html
          mirror -R ./main/templates templates
          mirror -R ./main/static static
          quit
          "
